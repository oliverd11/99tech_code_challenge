function sum_to_n_a(n: number): number {
  return ((n + 1) * n) / 2;
}

/**
 * Time Complexity: O(1): This solution has constant time complexity because it computes the sum using a direct mathematical formula.
 *
 * Space Complexity: O(1): This function only requires a constant amount of space for the variables n and the result.
 *
 */
